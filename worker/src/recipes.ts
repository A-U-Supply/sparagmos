/**
 * DO NOT EDIT -- this file is auto-generated by scripts/generate_worker_recipes.py
 * from recipes/*.yaml. To update, run: python scripts/generate_worker_recipes.py
 */

/** A sparagmos recipe definition. */
export interface Recipe {
  slug: string;
  inputs: number;
  name: string;
  description: string;
  effects: string;
}

/** All available recipes with their required input counts. */
export const RECIPES: readonly Recipe[] = [
  { slug: "acid-wash", inputs: 3, name: "Acid Wash", description: "Three images run through spectral transformation — their frequencies scrambled and redistributed...", effects: "spectral(a) → spectral(b) → pixel_sort(c) → blend(a,b→canvas) → blend(canvas,c→canvas) → pixel_sort(canvas) → dither(canvas) → jpeg_destroy(canvas)" },
  { slug: "ancients-bullethole", inputs: 3, name: "Ancients Bullet Hole", description: "Random circular cut-and-rotate holes punched into two images at high chaos before stencil composi...", effects: "ancient_bullethole(a) → ancient_bullethole(b) → ancient_stencil(c,a,b→canvas)" },
  { slug: "ancients-bullseye", inputs: 3, name: "Ancients Bullseye", description: "Concentric circle cut-and-rotate applied to two images before stencil compositing.", effects: "ancient_bullseye(a) → ancient_bullseye(b) → ancient_stencil(c,a,b→canvas)" },
  { slug: "ancients-collage", inputs: 4, name: "Ancients Collage", description: "Quadrant-mixed composites with geometric transformation and neural seam inpainting.", effects: "ancient_collage(a,b,c,d→canvas)" },
  { slug: "ancients-curvylinescreen", inputs: 3, name: "Ancients Curvy Line Screen", description: "Edge-following line screen stencil.", effects: "ancient_curvylinescreen(a,b,c→canvas)" },
  { slug: "ancients-cyanotype", inputs: 3, name: "Ancients Cyanotype", description: "Prussian blue cyanotype treatment before stencil compositing.", effects: "ancient_cyanotype(a,b,c→canvas)" },
  { slug: "ancients-displacement", inputs: 3, name: "Ancients Displacement", description: "Gradient-based displacement stencil.", effects: "ancient_displacement(a,b,c→canvas)" },
  { slug: "ancients-halationedge", inputs: 3, name: "Ancients Halation Edge", description: "Stencil compositing with organic grain at edge boundaries.", effects: "ancient_halationedge(a,b,c→canvas)" },
  { slug: "ancients-halftone", inputs: 3, name: "Ancients Halftone", description: "AM halftone dot screen stencil.", effects: "ancient_halftone(a,b,c→canvas)" },
  { slug: "ancients-kaleidoscope", inputs: 3, name: "Ancients Kaleidoscope", description: "N-fold radial symmetry applied to bullseye ring structure on two images before stencil compositing.", effects: "ancient_kaleidoscope(a) → ancient_kaleidoscope(b) → ancient_stencil(c,a,b→canvas)" },
  { slug: "ancients-lathe", inputs: 3, name: "Ancients Lathe", description: "Dozens of thin concentric ring bands, each independently rotated, applied to two images before st...", effects: "ancient_lathe(a) → ancient_lathe(b) → ancient_stencil(c,a,b→canvas)" },
  { slug: "ancients-linescreen", inputs: 3, name: "Ancients Line Screen", description: "Fixed-angle parallel line screen stencil.", effects: "ancient_linescreen(a,b,c→canvas)" },
  { slug: "ancients-quad", inputs: 4, name: "Ancients Quad", description: "Three-level stencil compositing.", effects: "ancient_quad(a,b,c,d→canvas)" },
  { slug: "ancients-reducedlinescreen", inputs: 3, name: "Ancients Reduced Line Screen", description: "Adaptive-frequency horizontal line screen stencil.", effects: "ancient_reducedlinescreen(a,b,c→canvas)" },
  { slug: "ancients-silver", inputs: 3, name: "Ancients Silver", description: "Silver gelatin print treatment before stencil compositing.", effects: "ancient_silver(a,b,c→canvas)" },
  { slug: "ancients-sixshooter", inputs: 3, name: "Ancients Six Shooter", description: "Six circular cut-and-rotate operations in grid or hexagonal layout applied to two images.", effects: "ancient_sixshooter(a) → ancient_sixshooter(b) → ancient_stencil(c,a,b→canvas)" },
  { slug: "ancients-stencil", inputs: 3, name: "Ancients Stencil", description: "Binary mask compositing via Otsu's thresholding.", effects: "ancient_stencil(a,b,c→canvas)" },
  { slug: "ancients-wobbleeye", inputs: 3, name: "Ancients Wobble Eye", description: "Off-axis concentric ring cut-and-rotate applied to two images before stencil compositing.", effects: "ancient_wobbleeye(a) → ancient_wobbleeye(b) → ancient_stencil(c,a,b→canvas)" },
  { slug: "binary-archaeology", inputs: 4, name: "Binary Archaeology", description: "Each of the four images is dithered to a different binary palette — different dithering algorithm...", effects: "dither(a) → dither(b) → dither(c) → dither(d) → mask_composite(b,a→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,d→canvas) → mask_composite(canvas,a→canvas) → byte_corrupt(canvas) → jpeg_destroy(canvas)" },
  { slug: "broadcast-static", inputs: 3, name: "Broadcast Static", description: "Three images converted to sound and back — each through a different acoustic treatment, each emer...", effects: "sonify(a) → sonify(b) → sonify(c) → collage(a,b,c→canvas) → crt_vhs(canvas) → byte_corrupt(canvas) → jpeg_destroy(canvas)" },
  { slug: "broken-mirror", inputs: 5, name: "Broken Mirror", description: "Five images shattered along Voronoi seams with deliberate gaps, as if the mirror holding all five...", effects: "channel_shift(a) → channel_shift(b) → channel_shift(c) → pixel_sort(d) → pixel_sort(e) → fragment(a,b,c,d,e→canvas) → channel_shift(canvas) → pixel_sort(canvas) → jpeg_destroy(canvas)" },
  { slug: "bullethole-silver", inputs: 3, name: "Bullet Hole Silver", description: "Two images riddled with circular punch holes at high chaos — perforated surfaces — then composite...", effects: "ancient_bullethole(a) → ancient_bullethole(b) → ancient_silver(c,a,b→canvas)" },
  { slug: "bullseye-cyanotype", inputs: 3, name: "Bullseye Cyanotype", description: "Two images bullseyed — concentric rings cut and independently rotated — then all three composited...", effects: "ancient_bullseye(a) → ancient_bullseye(b) → ancient_cyanotype(c,a,b→canvas) → jpeg_destroy(canvas)" },
  { slug: "bullseye-halftone-fragment", inputs: 5, name: "Bullseye Halftone Fragment", description: "Five images: two bullseyed into concentric ring targets, halftone- stenciled through a third.", effects: "ancient_bullseye(a) → ancient_bullseye(b) → ancient_halftone(c,a,b→canvas) → fragment(canvas,d,e→canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "bullseye-wobble-displacement", inputs: 3, name: "Bullseye Wobble Displacement", description: "Double concentric destruction: one image bullseyed into clean rings, another wobble-eyed into dri...", effects: "ancient_bullseye(a) → ancient_wobbleeye(b) → ancient_displacement(c,a,b→canvas)" },
  { slug: "chimera-engine", inputs: 5, name: "Chimera Engine", description: "Three images Voronoi-fragmented into organic cells, blended with a fourth, then edge-masked again...", effects: "deepdream(a) → pixel_sort(b) → dither(c) → crt_vhs(d) → channel_shift(e) → fragment(a,b,c→canvas) → blend(canvas,d→canvas) → mask_composite(canvas,e→canvas) → channel_shift(canvas) → fragment(canvas,a→canvas) → jpeg_destroy(canvas)" },
  { slug: "collage-mask-collage", inputs: 4, name: "Collage Mask Collage", description: "Two images collaged into a mosaic grid, then a third used as an edge stencil to cut through the g...", effects: "dither(a) → channel_shift(b) → collage(a,b→mosaic) → mask_composite(mosaic,c→canvas) → collage(canvas,d→canvas) → mask_composite(canvas,mosaic→canvas) → mask_composite(canvas,d→canvas) → dither(canvas) → jpeg_destroy(canvas)" },
  { slug: "collage-surgery", inputs: 4, name: "Collage Surgery", description: "Four images cut into strips and edge-masked three times in alternating rounds — each pass strips...", effects: "pixel_sort(a) → spectral(b) → crt_vhs(c) → channel_shift(d) → fragment(a,b→canvas) → mask_composite(canvas,c→canvas) → channel_shift(canvas) → fragment(canvas,d→canvas) → mask_composite(canvas,a→canvas) → channel_shift(canvas) → mask_composite(canvas,b→canvas) → jpeg_destroy(canvas)" },
  { slug: "collage-through-glass", inputs: 4, name: "Collage Through Glass", description: "Three images are mosaiced into a composite grid — an even tiling of three borrowed worlds.", effects: "pixel_sort(a) → crt_vhs(b) → dither(c) → pca_decompose(d) → collage(a,b,c→canvas) → mask_composite(canvas,d→canvas) → seam_carve(canvas) → channel_shift(canvas) → spectral(canvas) → jpeg_destroy(canvas)" },
  { slug: "collage-veil", inputs: 5, name: "Collage Veil", description: "All five images are first mosaiced into a single tiled composite — five worlds locked into one grid.", effects: "pixel_sort(a) → dither(b) → crt_vhs(c) → channel_shift(d) → spectral(e) → collage(a,b,c,d,e→canvas) → mask_composite(canvas,b→canvas) → mask_composite(canvas,c→canvas) → sonify(canvas) → mask_composite(canvas,d→canvas) → mask_composite(canvas,e→canvas) → byte_corrupt(canvas)" },
  { slug: "composite-quilt", inputs: 5, name: "Composite Quilt", description: "Images A and B are grid-fragmented into rectangular patches — a quilt of borrowed tiles from two...", effects: "pixel_sort(a) → crt_vhs(b) → spectral(c) → dither(d) → pca_decompose(e) → fragment(a,b→canvas) → mask_composite(canvas,c→canvas) → fragment(canvas,d→canvas) → mask_composite(canvas,e→canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "contrast-surgery", inputs: 4, name: "Contrast Surgery", description: "Image A undergoes extreme channel surgery — massive RGB offsets pull its color registers apart un...", effects: "channel_shift(a) → spectral(b) → pca_decompose(c) → pixel_sort(d) → mask_composite(b,a→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,b→canvas) → blend(canvas,d→canvas) → spectral(canvas) → byte_corrupt(canvas)" },
  { slug: "cross-pollinate", inputs: 5, name: "Cross Pollinate", description: "Two parallel edge-mask chains cross-pollinate their results.", effects: "channel_shift(a) → dither(b) → spectral(c) → crt_vhs(d) → pca_decompose(e) → mask_composite(b,a→pair_ab) → mask_composite(d,c→pair_cd) → blend(pair_ab,pair_cd→canvas) → mask_composite(canvas,e→canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "curvy-corruption", inputs: 3, name: "Curvy Corruption", description: "Three images composited through edge-following curvy line screens, then the result is byte-corrup...", effects: "ancient_curvylinescreen(a,b,c→canvas) → byte_corrupt(canvas) → crt_vhs(canvas) → jpeg_destroy(canvas)" },
  { slug: "cut-paste-repeat", inputs: 5, name: "Cut Paste Repeat", description: "The name is the recipe: mask, paste, cut, paste, mask again.", effects: "pca_decompose(a) → crt_vhs(b) → dither(c) → pixel_sort(d) → channel_shift(e) → mask_composite(b,a→canvas) → fragment(canvas,c→canvas) → mask_composite(canvas,d→canvas) → collage(canvas,e→canvas) → mask_composite(canvas,a→canvas) → channel_shift(canvas) → byte_corrupt(canvas)" },
  { slug: "cutout-mosaic", inputs: 5, name: "Cutout Mosaic", description: "Three images are mosaiced into a grid composite — fragments locked into cells, a surface of borro...", effects: "dither(a) → pixel_sort(b) → crt_vhs(c) → dither(d) → channel_shift(e) → collage(a,b,c→canvas) → mask_composite(e,canvas→canvas) → mask_composite(canvas,e→canvas) → fragment(canvas,b→canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "cyanotype-bullethole-mask", inputs: 4, name: "Cyanotype Bullet Hole Mask", description: "Four images: two riddled with bullet holes, then cyanotype-stenciled.", effects: "ancient_bullethole(a) → ancient_bullethole(b) → ancient_cyanotype(c,a,b→canvas) → mask_composite(canvas,d→canvas) → jpeg_destroy(canvas)" },
  { slug: "cyanotype-corruption", inputs: 3, name: "Cyanotype Corruption", description: "Three images composited through cyanotype stencil — prussian blue toning with highlight bloom — t...", effects: "ancient_cyanotype(a,b,c→canvas) → byte_corrupt(canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "data-rot", inputs: 3, name: "Data Rot", description: "Three images subjected to byte-level corruption before any composition — the damage is baked in f...", effects: "byte_corrupt(a) → byte_corrupt(b) → byte_corrupt(c) → fragment(a,b,c→canvas) → byte_corrupt(canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "displacement-blend-spectral", inputs: 4, name: "Displacement Blend Spectral", description: "Four images: displacement-stenciled, then screen-blended with a fourth.", effects: "spectral(a) → ancient_displacement(b,a,c→canvas) → blend(canvas,d→canvas) → spectral(canvas) → dither(canvas)" },
  { slug: "displacement-dream-collage", inputs: 5, name: "Displacement Dream Collage", description: "Five images: two deepdreamed into neural hallucinations, displacement- stenciled through a third.", effects: "deepdream(a) → deepdream(b) → ancient_displacement(c,a,b→canvas) → collage(canvas,d,e→canvas) → mask_composite(canvas,d→canvas) → jpeg_destroy(canvas)" },
  { slug: "dissolve-cascade", inputs: 4, name: "Dissolve Cascade", description: "Four images dissolved in waves: screen blend accumulates the light, gradient masking carves the e...", effects: "pixel_sort(a) → channel_shift(b) → spectral(c) → crt_vhs(d) → blend(a,b→canvas) → mask_composite(canvas,c→canvas) → blend(canvas,d→canvas) → mask_composite(canvas,a→canvas) → collage(canvas,b→canvas) → jpeg_destroy(canvas) → channel_shift(canvas)" },
  { slug: "double-exposure", inputs: 2, name: "Double Exposure", description: "Two images burned onto the same film.", effects: "spectral(a) → sonify(b) → blend(a,b→canvas) → blend(canvas,a→canvas) → sonify(canvas) → jpeg_destroy(canvas)" },
  { slug: "double-masked-collage", inputs: 4, name: "Double Masked Collage", description: "A double masking sandwich: fragment, edge-mask, scatter-collage, luminance- mask again.", effects: "channel_shift(a) → spectral(b) → pca_decompose(c) → dither(d) → fragment(a,b→canvas) → mask_composite(canvas,c→canvas) → collage(canvas,d→canvas) → mask_composite(canvas,d→canvas) → byte_corrupt(canvas) → crt_vhs(canvas)" },
  { slug: "dream-archaeology", inputs: 5, name: "Dream Archaeology", description: "Each image is PCA-decomposed to different component depths — A stripped to near-structural ghost,...", effects: "pca_decompose(a) → pca_decompose(b) → pca_decompose(c) → pca_decompose(d) → deepdream(a) → deepdream(b) → deepdream(c) → deepdream(d) → collage(a,b,c,d,e→canvas) → mask_composite(canvas,e→canvas) → dither(canvas) → channel_shift(canvas)" },
  { slug: "dream-cutout", inputs: 3, name: "Dream Cutout", description: "Image A is deepdreamed heavily — the network projects its hallucinations across every surface.", effects: "deepdream(a) → mask_composite(a,b→canvas) → channel_shift(canvas) → mask_composite(canvas,c→canvas) → jpeg_destroy(canvas)" },
  { slug: "dream-dissolve", inputs: 2, name: "Dream Dissolve", description: "Two images each sent through deep neural hallucination — the network finds eyes and feathers in e...", effects: "deepdream(a) → deepdream(b) → blend(a,b→canvas) → mask_composite(canvas,b→canvas) → spectral(canvas) → jpeg_destroy(canvas)" },
  { slug: "dream-erosion", inputs: 4, name: "Dream Erosion", description: "A is deepdreamed heavily, colonized by neural hallucinations until the original photograph is bar...", effects: "deepdream(a) → mask_composite(b,a→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,d→canvas) → deepdream(canvas) → mask_composite(canvas,a→canvas) → deepdream(canvas) → byte_corrupt(canvas) → jpeg_destroy(canvas)" },
  { slug: "dream-fragment-storm", inputs: 5, name: "Dream Fragment Storm", description: "Three images are deepdreamed — A, B, and C each acquiring dense neural hallucinations.", effects: "deepdream(a) → deepdream(b) → deepdream(c) → fragment(a,b,c→canvas) → deepdream(canvas) → mask_composite(canvas,d→canvas) → collage(canvas,e→canvas) → mask_composite(canvas,b→canvas) → jpeg_destroy(canvas) → jpeg_destroy(canvas)" },
  { slug: "dream-halftone", inputs: 3, name: "Dream Halftone", description: "Three images deepdreamed until dense with neural hallucinations, then composited through AM halft...", effects: "deepdream(a) → deepdream(b) → ancient_halftone(a,b,c→canvas) → jpeg_destroy(canvas)" },
  { slug: "dream-mask-dream", inputs: 4, name: "Dream Mask Dream", description: "A triple dream-mask cycle.", effects: "deepdream(a) → mask_composite(a,b→canvas) → deepdream(canvas) → mask_composite(canvas,c→canvas) → deepdream(canvas) → mask_composite(canvas,d→canvas) → mask_composite(canvas,a→canvas) → jpeg_destroy(canvas)" },
  { slug: "dream-mask", inputs: 4, name: "Dream Mask", description: "Image A is deepdreamed heavily — the network hallucinates eyes, pagodas, dog-faces across every s...", effects: "deepdream(a) → deepdream(d) → mask_composite(b,a→canvas) → blend(canvas,c→canvas) → mask_composite(canvas,a→canvas) → mask_composite(canvas,d→canvas) → deepdream(canvas) → byte_corrupt(canvas) → jpeg_destroy(canvas)" },
  { slug: "dream-quad", inputs: 4, name: "Dream Quad", description: "Four images deepdreamed to varying intensities, then composited through three-level stencil.", effects: "deepdream(a) → deepdream(b) → deepdream(c) → ancient_quad(d,a,b,c→canvas) → jpeg_destroy(canvas)" },
  { slug: "dream-stencil", inputs: 4, name: "Dream Stencil", description: "A is deepdreamed at high intensity until its surface is dense with hallucinated forms — eyes insi...", effects: "deepdream(a) → mask_composite(b,a→canvas) → deepdream(canvas) → blend(canvas,c→canvas) → mask_composite(canvas,d→canvas) → mask_composite(canvas,a→canvas) → deepdream(canvas) → dither(canvas) → jpeg_destroy(canvas)" },
  { slug: "dream-within-dream", inputs: 3, name: "Dream Within Dream", description: "Recursive hallucination in three passes.", effects: "deepdream(a) → mask_composite(b,a→canvas) → deepdream(canvas) → mask_composite(canvas,c→canvas) → deepdream(canvas) → mask_composite(canvas,a→canvas) → pixel_sort(canvas) → jpeg_destroy(canvas)" },
  { slug: "edge-ghosts", inputs: 3, name: "Edge Ghosts", description: "Phantom outlines from one image haunting another.", effects: "spectral(a) → pca_decompose(b) → channel_shift(c) → mask_composite(a,b→canvas) → mask_composite(canvas,c→canvas) → spectral(canvas) → channel_shift(canvas)" },
  { slug: "edge-lattice", inputs: 5, name: "Edge Lattice", description: "Edge masking applied to every consecutive pair of five images — a cascades into b, b into c, c in...", effects: "channel_shift(a) → crt_vhs(b) → pca_decompose(c) → pixel_sort(d) → dither(e) → mask_composite(a,b→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,d→canvas) → mask_composite(canvas,e→canvas) → collage(canvas,a→canvas) → pixel_sort(canvas) → jpeg_destroy(canvas)" },
  { slug: "edge-weave", inputs: 5, name: "Edge Weave", description: "Five images woven together through their own structural edges.", effects: "pixel_sort(a) → crt_vhs(b) → dither(c) → spectral(d) → channel_shift(e) → mask_composite(b,a→weave_ab) → mask_composite(c,b→weave_bc) → mask_composite(d,c→weave_cd) → collage(weave_ab,weave_bc,weave_cd→canvas) → mask_composite(canvas,e→canvas) → spectral(canvas) → channel_shift(canvas) → byte_corrupt(canvas)" },
  { slug: "entropy-garden", inputs: 5, name: "Entropy Garden", description: "Five images each deepdreamed into proliferating hallucinations before being assembled into a mosaic.", effects: "deepdream(a) → deepdream(b) → deepdream(c) → deepdream(d) → deepdream(e) → collage(a,b,c,d,e→canvas) → mask_composite(canvas,a→canvas) → fragment(canvas,b→canvas) → blend(canvas,c→canvas) → jpeg_destroy(canvas)" },
  { slug: "exquisite-corpse", inputs: 3, name: "Exquisite Corpse", description: "The surrealist parlor game.", effects: "pixel_sort(a) → crt_vhs(b) → channel_shift(c) → fragment(a,b,c→canvas) → seam_carve(canvas) → crt_vhs(canvas) → jpeg_destroy(canvas)" },
  { slug: "feedback-loop", inputs: 2, name: "Feedback Loop", description: "Compose, destroy, re-compose the wreckage, destroy again.", effects: "blend(a,b→canvas) → imagemagick(canvas) → channel_shift(canvas) → fragment(canvas,a,b→canvas) → blend(canvas,b→canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "fossil-record", inputs: 3, name: "Fossil Record", description: "Geological strata.", effects: "pca_decompose(a) → pca_decompose(b) → pca_decompose(c) → blend(a,b→canvas) → blend(canvas,c→canvas) → dither(canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "fracture-and-mask", inputs: 4, name: "Fracture and Mask", description: "A shatter-fragment sandwich with edge and luminance masking on either side.", effects: "channel_shift(a) → spectral(b) → pca_decompose(c) → dither(d) → fragment(a,b→canvas) → mask_composite(canvas,c→canvas) → collage(canvas,c→canvas) → mask_composite(canvas,d→canvas) → pixel_sort(canvas) → byte_corrupt(canvas)" },
  { slug: "fragment-displacement", inputs: 5, name: "Fragment Displacement", description: "Five images: three Voronoi-fragmented into a shattered mosaic, then displacement-stenciled agains...", effects: "pixel_sort(a) → channel_shift(b) → fragment(a,b,c→canvas) → ancient_displacement(d,canvas,e→canvas) → jpeg_destroy(canvas)" },
  { slug: "fragment-storm", inputs: 5, name: "Fragment Storm", description: "Five images shattered twice with different piece counts — first a coarse shattering, then edge ma...", effects: "channel_shift(a) → dither(b) → pixel_sort(c) → crt_vhs(d) → pca_decompose(e) → fragment(a,b,c,d,e→canvas) → mask_composite(canvas,a→canvas) → fragment(canvas,b→canvas) → collage(canvas,c→canvas) → jpeg_destroy(canvas) → jpeg_destroy(canvas) → jpeg_destroy(canvas)" },
  { slug: "geometric-chaos", inputs: 5, name: "Geometric Chaos", description: "Five images each hit with a different geometric destruction — bullseye, wobbleeye, sixshooter, ka...", effects: "ancient_bullseye(a) → ancient_wobbleeye(b) → ancient_sixshooter(c) → ancient_kaleidoscope(d) → ancient_lathe(e) → collage(a,b,c,d,e→canvas) → mask_composite(canvas,a→canvas) → jpeg_destroy(canvas)" },
  { slug: "ghost-overlay", inputs: 2, name: "Ghost Overlay", description: "A haunting: A's edges become a soft veil cast over B with high feathering, so the transition is a...", effects: "mask_composite(a,b→canvas) → blend(canvas,a→canvas) → jpeg_destroy(canvas)" },
  { slug: "gradient-reveal", inputs: 4, name: "Gradient Reveal", description: "A three-stage revelation where each step peels back a different layer.", effects: "pixel_sort(a) → crt_vhs(b) → pca_decompose(c) → dither(d) → blend(a,b→blended) → mask_composite(blended,c→canvas) → fragment(canvas,b→canvas) → mask_composite(canvas,d→canvas) → pixel_sort(canvas) → spectral(canvas) → crt_vhs(canvas)" },
  { slug: "halftone-collage-shift", inputs: 5, name: "Halftone Collage Shift", description: "Five images: halftone-stenciled three ways, then mosaiced with the remaining two into a grid comp...", effects: "ancient_halftone(a,b,c→canvas) → collage(canvas,d,e→canvas) → channel_shift(canvas) → mask_composite(canvas,a→canvas) → jpeg_destroy(canvas)" },
  { slug: "halftone-curvyscreen", inputs: 3, name: "Halftone Curvy Screen", description: "Two-pass screen compositing: first, halftone dot screen composites two images.", effects: "ancient_halftone(a,b,c→canvas) → ancient_curvylinescreen(c,canvas,a→canvas)" },
  { slug: "hallucinated-quilt", inputs: 5, name: "Hallucinated Quilt", description: "Five images dreamed at different iteration counts — 5, 10, 15, 20, and 25 — creating a spectrum f...", effects: "deepdream(a) → deepdream(b) → deepdream(c) → deepdream(d) → deepdream(e) → collage(a,b,c,d,e→canvas) → fragment(canvas,c→canvas) → channel_shift(canvas) → spectral(canvas) → jpeg_destroy(canvas) → jpeg_destroy(canvas)" },
  { slug: "hallucination-collage", inputs: 5, name: "Hallucination Collage", description: "All five inputs are deepdreamed at escalating intensities — A barely touched at 5 iterations, B a...", effects: "deepdream(a) → deepdream(b) → deepdream(c) → deepdream(d) → deepdream(e) → collage(a,b,c,d,e→canvas) → fragment(canvas,e→canvas) → mask_composite(canvas,e→canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "holographic-interference", inputs: 3, name: "Holographic Interference", description: "Three images processed through spectral decomposition and channel aberration, then stacked via di...", effects: "spectral(a) → spectral(b) → channel_shift(c) → blend(a,b→canvas) → blend(canvas,c→canvas) → channel_shift(canvas) → spectral(canvas) → jpeg_destroy(canvas)" },
  { slug: "interference-mask", inputs: 4, name: "Interference Mask", description: "Images A and B are blended at difference mode — each pixel becomes the absolute distance between...", effects: "channel_shift(a) → spectral(b) → pca_decompose(c) → dither(d) → blend(a,b→diff) → mask_composite(c,diff→canvas) → collage(canvas,d→canvas) → mask_composite(canvas,b→canvas) → pixel_sort(canvas) → crt_vhs(canvas) → byte_corrupt(canvas)" },
  { slug: "kaleidoscope-collapse", inputs: 5, name: "Kaleidoscope Collapse", description: "Five sources collaged into a mosaic, then the mosaic is shattered and edge-masked against one of...", effects: "channel_shift(a) → pixel_sort(b) → crt_vhs(c) → pca_decompose(d) → dither(e) → collage(a,b,c,d,e→canvas) → fragment(canvas,a→canvas) → mask_composite(canvas,b→canvas) → fragment(canvas,c→canvas) → pixel_sort(canvas) → jpeg_destroy(canvas) → byte_corrupt(canvas)" },
  { slug: "kaleidoscope-cyanotype-pca", inputs: 4, name: "Kaleidoscope Cyanotype PCA", description: "Four images: two kaleidoscoped into mandala patterns, PCA-decomposed to structural ghosts, then c...", effects: "ancient_kaleidoscope(a) → pca_decompose(a) → ancient_kaleidoscope(b) → pca_decompose(b) → ancient_cyanotype(c,a,b→canvas) → mask_composite(canvas,d→canvas)" },
  { slug: "kaleidoscope-dream-displacement", inputs: 4, name: "Kaleidoscope Dream Displacement", description: "Four images: two kaleidoscoped into mandala symmetry, one deepdreamed until colonized by neural h...", effects: "ancient_kaleidoscope(a) → ancient_kaleidoscope(b) → deepdream(c) → ancient_displacement(d,a,c→canvas) → blend(canvas,b→canvas)" },
  { slug: "kaleidoscope-dream", inputs: 5, name: "Kaleidoscope Dream", description: "All five inputs are deepdreamed independently, each colonized by the network's preferred hallucin...", effects: "deepdream(a) → deepdream(b) → deepdream(c) → deepdream(d) → deepdream(e) → collage(a,b,c,d,e→canvas) → fragment(canvas,a→canvas) → mask_composite(canvas,d→canvas) → collage(canvas,c→canvas) → deepdream(canvas) → jpeg_destroy(canvas) → jpeg_destroy(canvas) → jpeg_destroy(canvas)" },
  { slug: "kaleidoscope-halftone", inputs: 3, name: "Kaleidoscope Halftone", description: "Two images kaleidoscoped into mandala patterns — radial symmetry folding their surfaces into repe...", effects: "ancient_kaleidoscope(a) → ancient_kaleidoscope(b) → ancient_halftone(c,a,b→canvas)" },
  { slug: "lathe-displacement", inputs: 3, name: "Lathe Displacement", description: "Two images lathe-turned — dozens of thin concentric rings each independently rotated into agate-l...", effects: "ancient_lathe(a) → ancient_lathe(b) → ancient_displacement(c,a,b→canvas)" },
  { slug: "lathe-kaleidoscope-stencil", inputs: 3, name: "Lathe Kaleidoscope Stencil", description: "Double geometric destruction: one image lathe-turned into agate rings, another kaleidoscoped into...", effects: "ancient_lathe(a) → ancient_kaleidoscope(b) → ancient_wobbleeye(c) → ancient_stencil(a,b,c→canvas)" },
  { slug: "lathe-silver-sonify", inputs: 4, name: "Lathe Silver Sonify", description: "Four images: two lathe-turned into agate ring surfaces, silver-stenciled through a third.", effects: "ancient_lathe(a) → ancient_lathe(b) → ancient_silver(c,a,b→canvas) → sonify(canvas) → mask_composite(canvas,d→canvas)" },
  { slug: "layered-cutwork", inputs: 4, name: "Layered Cutwork", description: "Three sequential rounds of mask then collage then mask: the image accumulates wounds with each pass.", effects: "pca_decompose(a) → crt_vhs(b) → pixel_sort(c) → dither(d) → mask_composite(b,a→canvas) → collage(canvas,c→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,d→canvas) → channel_shift(canvas) → seam_carve(canvas) → byte_corrupt(canvas)" },
  { slug: "letterpress", inputs: 4, name: "Letterpress", description: "Image A is dithered with Atkinson, which creates the tight, typographic dot-and-space pattern tha...", effects: "dither(a) → pixel_sort(b) → pca_decompose(c) → crt_vhs(d) → mask_composite(b,a→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,b→canvas) → fragment(canvas,d→canvas) → channel_shift(canvas) → byte_corrupt(canvas)" },
  { slug: "luminance-layers", inputs: 4, name: "Luminance Layers", description: "Four images revealed through four different luminance thresholds of the same reference source.", effects: "pca_decompose(a) → channel_shift(b) → dither(c) → crt_vhs(d) → mask_composite(b,a→band1) → mask_composite(c,a→band2) → mask_composite(d,a→band3) → collage(band1,band2,band3→canvas) → mask_composite(canvas,a→canvas) → seam_carve(canvas) → jpeg_destroy(canvas)" },
  { slug: "mask-and-glitch", inputs: 3, name: "Mask and Glitch", description: "Image A is edge-masked onto B, producing a clean structural composite.", effects: "mask_composite(a,b→canvas) → crt_vhs(canvas) → mask_composite(canvas,c→canvas) → byte_corrupt(canvas) → jpeg_destroy(canvas)" },
  { slug: "mask-and-scatter", inputs: 5, name: "Mask and Scatter", description: "Image A is luminance-masked onto B, cutting A's bright silhouette into B's flesh.", effects: "pca_decompose(a) → channel_shift(b) → dither(c) → pixel_sort(d) → crt_vhs(e) → mask_composite(b,a→canvas) → fragment(canvas,a→canvas) → collage(canvas,c,d→canvas) → mask_composite(canvas,e→canvas) → spectral(canvas) → jpeg_destroy(canvas)" },
  { slug: "mask-cascade", inputs: 4, name: "Mask Cascade", description: "Four images layered through four successive mask operations, each using a different mask source —...", effects: "pca_decompose(a) → spectral(b) → pixel_sort(c) → crt_vhs(d) → mask_composite(a,b→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,d→canvas) → mask_composite(canvas,a→canvas) → channel_shift(canvas) → dither(canvas) → byte_corrupt(canvas)" },
  { slug: "mask-collage-fusion", inputs: 5, name: "Mask Collage Fusion", description: "Image A is luminance-masked onto B — A's bright zones punch through to reveal B beneath, a hard s...", effects: "pca_decompose(a) → pixel_sort(b) → crt_vhs(c) → dither(d) → channel_shift(e) → mask_composite(b,a→canvas) → collage(canvas,c,d→canvas) → mask_composite(canvas,e→canvas) → pixel_sort(canvas) → mask_composite(canvas,b→canvas) → byte_corrupt(canvas)" },
  { slug: "mask-erosion", inputs: 4, name: "Mask Erosion", description: "Three sequential edge masks with increasing feather — a progressive erosion where each pass softe...", effects: "channel_shift(a) → spectral(b) → dither(c) → pca_decompose(d) → mask_composite(b,a→canvas) → mask_composite(canvas,c→canvas) → fragment(canvas,d→canvas) → mask_composite(canvas,a→canvas) → crt_vhs(canvas) → jpeg_destroy(canvas)" },
  { slug: "mask-everything", inputs: 5, name: "Mask Everything", description: "Maximum masking: every image pair gets composited through a mask before anything else happens.", effects: "mask_composite(a,b→pair_ab) → mask_composite(c,d→pair_cd) → blend(pair_ab,pair_cd→canvas) → mask_composite(canvas,e→canvas) → mask_composite(canvas,pair_ab→canvas) → fragment(canvas,e→canvas) → mask_composite(canvas,c→canvas) → jpeg_destroy(canvas)" },
  { slug: "mask-feedback", inputs: 5, name: "Mask Feedback", description: "A chain of masks where each output becomes the input mask for the next stage.", effects: "channel_shift(a) → spectral(b) → dither(c) → pixel_sort(d) → pca_decompose(e) → mask_composite(b,a→stage1) → channel_shift(stage1) → spectral(stage1) → mask_composite(c,stage1→stage2) → pixel_sort(stage2) → byte_corrupt(stage2) → mask_composite(d,stage2→canvas) → blend(canvas,e→canvas) → crt_vhs(canvas)" },
  { slug: "mask-fragment-cycle", inputs: 5, name: "Mask Fragment Cycle", description: "A full cycle: mask, then cut, then mask again, then paste.", effects: "pca_decompose(a) → channel_shift(b) → crt_vhs(c) → dither(d) → pixel_sort(e) → mask_composite(b,a→canvas) → fragment(canvas,c→canvas) → mask_composite(canvas,d→canvas) → collage(canvas,e→canvas) → pixel_sort(canvas) → sonify(canvas) → jpeg_destroy(canvas)" },
  { slug: "masked-archaeology", inputs: 5, name: "Masked Archaeology", description: "Each input is PCA-decomposed to a different depth — A stripped to 1 principal component (near-pur...", effects: "pca_decompose(a) → pca_decompose(b) → pca_decompose(c) → pca_decompose(d) → channel_shift(e) → mask_composite(b,a→canvas) → collage(canvas,c,d→canvas) → mask_composite(canvas,e→canvas) → dither(canvas) → byte_corrupt(canvas) → channel_shift(canvas)" },
  { slug: "masked-tapestry", inputs: 5, name: "Masked Tapestry", description: "Five images woven into a layered tapestry through successive masking.", effects: "pixel_sort(a) → crt_vhs(b) → dither(c) → pca_decompose(d) → channel_shift(e) → collage(a,b,c→canvas) → mask_composite(canvas,d→canvas) → fragment(canvas,d→canvas) → mask_composite(canvas,e→canvas) → channel_shift(canvas) → sonify(canvas) → jpeg_destroy(canvas)" },
  { slug: "mosaic-dissolution", inputs: 5, name: "Mosaic Dissolution", description: "Five sources tiled into a mosaic, then the mosaic itself is fragmented and pixel-sorted into dust.", effects: "channel_shift(a) → crt_vhs(b) → dither(c) → pixel_sort(d) → pca_decompose(e) → collage(a,b,c,d,e→canvas) → fragment(canvas,a→canvas) → pixel_sort(canvas) → jpeg_destroy(canvas) → byte_corrupt(canvas)" },
  { slug: "mosaic-rebirth", inputs: 5, name: "Mosaic Rebirth", description: "Five sources assembled into a mosaic, sorted by pixel luminance into streaming vertical rivers, t...", effects: "channel_shift(a) → deepdream(b) → pca_decompose(c) → crt_vhs(d) → dither(e) → collage(a,b,c,d,e→canvas) → pixel_sort(canvas) → fragment(canvas,a→canvas) → mask_composite(canvas,b→canvas) → collage(canvas,c→canvas) → jpeg_destroy(canvas)" },
  { slug: "negative-space", inputs: 4, name: "Negative Space", description: "Two masking operations, each using the opposite of what the other reveals.", effects: "dither(a) → spectral(b) → dither(c) → channel_shift(d) → mask_composite(b,a→dark_cut) → mask_composite(d,c→light_cut) → blend(dark_cut,light_cut→canvas) → mask_composite(canvas,dark_cut→canvas) → pixel_sort(canvas) → jpeg_destroy(canvas)" },
  { slug: "neural-chimera", inputs: 3, name: "Neural Chimera", description: "Three images deepdreamed at different neural layers then Voronoi-fragmented together.", effects: "deepdream(a) → deepdream(b) → deepdream(c) → fragment(a,b,c→canvas) → blend(canvas,b→canvas) → style_transfer(canvas) → jpeg_destroy(canvas)" },
  { slug: "neural-mask-cascade", inputs: 4, name: "Neural Mask Cascade", description: "Four images, each deepdreamed independently, then chained through a cascade of dream-as-mask oper...", effects: "deepdream(a) → deepdream(b) → deepdream(c) → deepdream(d) → mask_composite(b,a→canvas) → mask_composite(canvas,b→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,d→canvas) → pixel_sort(canvas) → jpeg_destroy(canvas)" },
  { slug: "neural-tapestry", inputs: 5, name: "Neural Tapestry", description: "Three of the five inputs are deepdreamed — A, B, and C each acquire the network's hallucinated fo...", effects: "deepdream(a) → deepdream(b) → deepdream(c) → fragment(a,b,c→canvas) → mask_composite(canvas,d→canvas) → collage(canvas,e→canvas) → deepdream(canvas) → spectral(canvas) → mask_composite(canvas,b→canvas) → jpeg_destroy(canvas)" },
  { slug: "noise-cathedral", inputs: 4, name: "Noise Cathedral", description: "Three noise masks at escalating thresholds cut random windows between four images, building layer...", effects: "pca_decompose(a) → spectral(b) → deepdream(c) → pixel_sort(d) → mask_composite(a,b→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,d→canvas) → fragment(canvas,a→canvas) → channel_shift(canvas) → byte_corrupt(canvas)" },
  { slug: "palimpsest-ii", inputs: 5, name: "Palimpsest II", description: "The manuscript overwritten so many times the original is unreadable.", effects: "pca_decompose(a) → channel_shift(b) → crt_vhs(c) → pixel_sort(d) → dither(e) → mask_composite(a,b→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,d→canvas) → mask_composite(canvas,e→canvas) → dither(canvas) → byte_corrupt(canvas) → jpeg_destroy(canvas)" },
  { slug: "palimpsest", inputs: 4, name: "Palimpsest", description: "A manuscript overwritten and overwritten again.", effects: "pca_decompose(a) → channel_shift(b) → crt_vhs(c) → pixel_sort(d) → mask_composite(a,b→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,d→canvas) → byte_corrupt(canvas) → dither(canvas)" },
  { slug: "pca-quad", inputs: 4, name: "PCA Quad", description: "Four images PCA-decomposed to varying depths — stripped to structural ghosts — then composited th...", effects: "pca_decompose(a) → pca_decompose(b) → pca_decompose(c) → ancient_quad(d,a,b,c→canvas) → pixel_sort(canvas) → byte_corrupt(canvas)" },
  { slug: "phantom-dream", inputs: 4, name: "Phantom Dream", description: "A and B are both deepdreamed — two photographs each colonized by the network's hallucinations ind...", effects: "deepdream(a) → deepdream(b) → blend(a,b→canvas) → mask_composite(canvas,c→canvas) → fragment(canvas,d→canvas) → deepdream(canvas) → mask_composite(canvas,d→canvas) → jpeg_destroy(canvas) → byte_corrupt(canvas)" },
  { slug: "phantom-limb", inputs: 2, name: "Phantom Limb", description: "One image's edge structure is cut out and laid over another — not the image itself, only its outl...", effects: "spectral(a) → pca_decompose(b) → mask_composite(a,b→canvas) → blend(canvas,a→canvas) → spectral(canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "photogram", inputs: 4, name: "Photogram", description: "In the darkroom, a photogram is made by placing objects directly on light-sensitive paper and exp...", effects: "pca_decompose(a) → spectral(b) → pca_decompose(c) → dither(d) → mask_composite(b,a→canvas) → pca_decompose(canvas) → mask_composite(c,canvas→canvas) → mask_composite(canvas,d→canvas) → mask_composite(canvas,b→canvas) → byte_corrupt(canvas) → jpeg_destroy(canvas)" },
  { slug: "quad-sort-destroy", inputs: 4, name: "Quad Sort Destroy", description: "Four images three-level stenciled — brightness bands filling with different sources — then the co...", effects: "ancient_quad(a,b,c,d→canvas) → pixel_sort(canvas) → byte_corrupt(canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "recursive-stencil", inputs: 5, name: "Recursive Stencil", description: "Five-deep recursive stenciling where each image's PCA structure is used as a luminance mask for t...", effects: "pca_decompose(a) → pca_decompose(b) → pca_decompose(c) → pca_decompose(d) → pca_decompose(e) → mask_composite(b,a→stage_ab) → mask_composite(c,stage_ab→stage_abc) → mask_composite(d,stage_abc→stage_abcd) → mask_composite(e,stage_abcd→canvas) → collage(canvas,stage_ab,stage_abc→canvas) → pixel_sort(canvas) → byte_corrupt(canvas)" },
  { slug: "reducedscreen-datamosh", inputs: 3, name: "Reduced Screen Datamosh", description: "Three images composited through adaptive-frequency line screens — finer lines in detail regions,...", effects: "ancient_reducedlinescreen(a,b,c→canvas) → datamosh(canvas) → channel_shift(canvas)" },
  { slug: "sediment-core", inputs: 3, name: "Sediment Core", description: "Three images arranged as horizontal strata — the slow accumulation of time rendered as a geologic...", effects: "pixel_sort(a) → spectral(b) → pca_decompose(c) → collage(a,b,c→canvas) → seam_carve(canvas) → blend(canvas,b→canvas) → dither(canvas) → jpeg_destroy(canvas)" },
  { slug: "shape-imposition", inputs: 5, name: "Shape Imposition", description: "Image A is PCA-reduced to a single principal component — its entire visual complexity collapsed t...", effects: "pca_decompose(a) → channel_shift(b) → pca_decompose(c) → dither(d) → pixel_sort(e) → mask_composite(b,a→canvas) → mask_composite(canvas,c→canvas) → collage(canvas,d,e→canvas) → mask_composite(canvas,b→canvas) → cellular(canvas) → jpeg_destroy(canvas)" },
  { slug: "shifted-linescreen", inputs: 3, name: "Shifted Line Screen", description: "Three images channel-shifted until their color registers separate, then composited through parall...", effects: "channel_shift(a) → channel_shift(b) → ancient_linescreen(c,a,b→canvas) → jpeg_destroy(canvas)" },
  { slug: "signal-bleed", inputs: 3, name: "Signal Bleed", description: "Three VHS signals bleeding into each other on a bad splitter.", effects: "crt_vhs(a) → crt_vhs(b) → crt_vhs(c) → collage(a,b,c→canvas) → channel_shift(canvas) → byte_corrupt(canvas) → jpeg_destroy(canvas)" },
  { slug: "silhouette-collage", inputs: 4, name: "Silhouette Collage", description: "Image A is PCA-decomposed to 2 principal components — stripped to the barest structural silhouett...", effects: "pca_decompose(a) → channel_shift(b) → crt_vhs(c) → pixel_sort(d) → mask_composite(b,a→canvas) → collage(canvas,c→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,d→canvas) → sonify(canvas) → channel_shift(canvas) → spectral(canvas)" },
  { slug: "silhouette-swap", inputs: 5, name: "Silhouette Swap", description: "A chain of five images where each one's PCA silhouette is used as the hard mask to cut the next.", effects: "pca_decompose(a) → pca_decompose(b) → pca_decompose(c) → pca_decompose(d) → mask_composite(b,a→ab) → mask_composite(c,ab→abc) → mask_composite(d,abc→abcd) → mask_composite(e,abcd→canvas) → collage(canvas,ab,abc→canvas) → pixel_sort(canvas) → byte_corrupt(canvas)" },
  { slug: "silver-displacement-dream", inputs: 4, name: "Silver Displacement Dream", description: "Four images: one deepdreamed heavily, silver-stenciled with two others.", effects: "deepdream(a) → ancient_silver(b,a,c→canvas) → ancient_displacement(d,canvas,a→canvas) → dither(canvas)" },
  { slug: "silver-lathe-fragment", inputs: 5, name: "Silver Lathe Fragment", description: "Five images: two lathe-turned into agate rings, three Voronoi-fragmented into shattered cells.", effects: "ancient_lathe(a) → ancient_lathe(b) → ancient_silver(c,a,b→canvas) → fragment(d,e,canvas→canvas) → pixel_sort(canvas)" },
  { slug: "simple-cutout", inputs: 2, name: "Simple Cutout", description: "The most direct use of masking: A's edge skeleton is cut clean into B.", effects: "mask_composite(a,b→canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "sixshooter-halation-corrupt", inputs: 4, name: "Six Shooter Halation Corrupt", description: "Four images: two six-shootered into scattered circular displacements, halation-edge composited th...", effects: "ancient_sixshooter(a) → ancient_sixshooter(b) → ancient_halationedge(c,a,b→canvas) → byte_corrupt(canvas) → mask_composite(canvas,d→canvas)" },
  { slug: "sixshooter-linescreen", inputs: 3, name: "Six Shooter Line Screen", description: "Two images six-shootered — six circular regions cut, shuffled, and rotated — then composited thro...", effects: "ancient_sixshooter(a) → ancient_sixshooter(b) → ancient_linescreen(c,a,b→canvas) → channel_shift(canvas)" },
  { slug: "sorted-displacement", inputs: 3, name: "Sorted Displacement", description: "Two images pixel-sorted to extreme thresholds — their surfaces sheared into vertical streaks — th...", effects: "pixel_sort(a) → pixel_sort(b) → ancient_displacement(c,a,b→canvas) → channel_shift(canvas)" },
  { slug: "sorted-mask", inputs: 3, name: "Sorted Mask", description: "Image A is pixel-sorted by brightness, generating long streaks that streak upward through the frame.", effects: "pixel_sort(a) → mask_composite(a,b→canvas) → collage(canvas,c→canvas) → mask_composite(canvas,a→canvas) → jpeg_destroy(canvas)" },
  { slug: "spectral-mask", inputs: 4, name: "Spectral Mask", description: "Four images processed spectrally and then masked in pairs, with the pair results blended and dith...", effects: "spectral(a) → spectral(b) → spectral(c) → spectral(d) → mask_composite(b,a→pair_ab) → mask_composite(d,c→pair_cd) → blend(pair_ab,pair_cd→canvas) → mask_composite(canvas,a→canvas) → dither(canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "spectral-merge", inputs: 2, name: "Spectral Merge", description: "Two signals in frequency space, destructively interfered.", effects: "spectral(a) → spectral(b) → blend(a,b→canvas) → mask_composite(canvas,a→canvas) → sonify(canvas) → dither(canvas)" },
  { slug: "spectral-silver", inputs: 3, name: "Spectral Silver", description: "Three images bandpass-filtered in frequency space — stripped to their mid-range structure, ghostl...", effects: "spectral(a) → spectral(b) → ancient_silver(a,b,c→canvas) → dither(canvas)" },
  { slug: "spectral-stencil", inputs: 3, name: "Spectral Stencil", description: "Image A is bandpass-filtered in frequency space, stripping its low and high frequencies to leave...", effects: "spectral(a) → mask_composite(a,b→canvas) → sonify(canvas) → mask_composite(canvas,c→canvas) → dither(canvas) → jpeg_destroy(canvas)" },
  { slug: "stamp-collection", inputs: 5, name: "Stamp Collection", description: "Five images are each fragmented into irregular pieces — the first pass shatters them into raw fra...", effects: "fragment(a,b→ab_frag) → fragment(c,d→cd_frag) → dither(e) → mask_composite(b,ab_frag→canvas) → mask_composite(canvas,cd_frag→canvas) → mask_composite(canvas,e→canvas) → mask_composite(canvas,ab_frag→canvas) → collage(canvas,cd_frag,e→canvas) → byte_corrupt(canvas) → jpeg_destroy(canvas)" },
  { slug: "stencil-burn", inputs: 4, name: "Stencil Burn", description: "Image A is dithered to a near-binary stencil — its shapes reduced to stark positive and negative...", effects: "dither(a) → channel_shift(b) → pixel_sort(c) → pca_decompose(d) → mask_composite(b,c→canvas) → mask_composite(canvas,d→canvas) → mask_composite(canvas,b→canvas) → pixel_sort(canvas) → byte_corrupt(canvas) → jpeg_destroy(canvas)" },
  { slug: "stencil-into-displacement", inputs: 4, name: "Stencil Into Displacement", description: "Four images in two stages: first, Otsu stencil composites images A and B using C as the mask.", effects: "ancient_stencil(a,b,c→canvas) → ancient_displacement(d,canvas,a→canvas) → jpeg_destroy(canvas)" },
  { slug: "stencil-mosaic", inputs: 5, name: "Stencil Mosaic", description: "Image A is dithered to thermal — reduced to pure heat-map extremes — then used as a hard threshol...", effects: "dither(a) → spectral(b) → channel_shift(c) → pca_decompose(d) → pixel_sort(e) → mask_composite(b,a→canvas) → fragment(canvas,c→canvas) → mask_composite(canvas,d→canvas) → collage(canvas,e→canvas) → spectral(canvas) → jpeg_destroy(canvas)" },
  { slug: "stratum-shift", inputs: 4, name: "Stratum Shift", description: "Four images built into geological strata through strip collage, then seam-carved to warp the faul...", effects: "channel_shift(a) → dither(b) → pca_decompose(c) → pixel_sort(d) → collage(a,b,c,d→canvas) → seam_carve(canvas) → mask_composite(canvas,a→canvas) → blend(canvas,b→canvas) → mask_composite(canvas,c→canvas) → dither(canvas) → byte_corrupt(canvas)" },
  { slug: "structural-echo", inputs: 4, name: "Structural Echo", description: "Image A's edge structure is applied three times to create a progressive echo effect.", effects: "pca_decompose(a) → channel_shift(b) → dither(c) → spectral(d) → mask_composite(b,a→echo_tight) → mask_composite(c,a→echo_mid) → mask_composite(d,a→echo_wide) → collage(echo_tight,echo_mid,echo_wide→canvas) → blend(canvas,echo_mid→canvas) → spectral(canvas) → pixel_sort(canvas) → byte_corrupt(canvas)" },
  { slug: "surveillance-decay", inputs: 4, name: "Surveillance Decay", description: "Four feeds arranged in the classic CCTV quad-view — the administrative gaze that watches everythi...", effects: "crt_vhs(a) → crt_vhs(b) → crt_vhs(c) → crt_vhs(d) → collage(a,b,c,d→canvas) → crt_vhs(canvas) → byte_corrupt(canvas) → jpeg_destroy(canvas)" },
  { slug: "tectonic-overlap", inputs: 4, name: "Tectonic Overlap", description: "Continental plates of image data, shifted and overlapping.", effects: "imagemagick(a) → deepdream(b) → sonify(c) → dither(d) → fragment(a,b,c,d→canvas) → collage(canvas,a→canvas) → seam_carve(canvas) → jpeg_destroy(canvas)" },
  { slug: "text-ghost", inputs: 4, name: "Text Ghost", description: "Image A is PCA-decomposed to just 1-2 principal components — its texture and color stripped away...", effects: "pca_decompose(a) → channel_shift(b) → spectral(c) → dither(d) → mask_composite(b,a→canvas) → blend(canvas,c→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,b→canvas) → crt_vhs(canvas) → jpeg_destroy(canvas)" },
  { slug: "threshold-cascade", inputs: 4, name: "Threshold Cascade", description: "Four mask_composite operations at four ascending threshold bands — 40, 80, 150, and 200 — each cu...", effects: "channel_shift(a) → dither(b) → pixel_sort(c) → spectral(d) → mask_composite(a,b→canvas) → mask_composite(canvas,c→canvas) → mask_composite(canvas,d→canvas) → mask_composite(canvas,a→canvas) → pixel_sort(canvas) → sonify(canvas) → crt_vhs(canvas)" },
  { slug: "torn-collage", inputs: 4, name: "Torn Collage", description: "Four images shattered like a mirror dropped on concrete.", effects: "crt_vhs(a) → crt_vhs(b) → crt_vhs(c) → crt_vhs(d) → fragment(a,b,c,d→canvas) → collage(canvas,a→canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "total-chaos", inputs: 5, name: "Total Chaos", description: "Every technique thrown into one sequence.", effects: "deepdream(a) → mask_composite(a,b→canvas) → collage(canvas,c→canvas) → fragment(canvas,d→canvas) → mask_composite(canvas,e→canvas) → blend(canvas,a→canvas) → mask_composite(canvas,e→canvas) → pixel_sort(canvas) → mask_composite(canvas,b→canvas) → jpeg_destroy(canvas)" },
  { slug: "triple-mask", inputs: 3, name: "Triple Mask", description: "Three images, two mask operations.", effects: "mask_composite(a,b→canvas) → mask_composite(canvas,c→canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "triple-stencil-cascade", inputs: 5, name: "Triple Stencil Cascade", description: "Five images in three stencil passes: first, Otsu stencil composites A and B through C.", effects: "ancient_stencil(a,b,c→canvas) → ancient_halftone(c,canvas,d→canvas) → ancient_displacement(e,canvas,a→canvas) → jpeg_destroy(canvas)" },
  { slug: "two-face", inputs: 2, name: "Two Face", description: "A luminance mask splits image A at its median brightness — the bright half stays A, the dark half...", effects: "mask_composite(a,b→canvas) → pixel_sort(canvas) → jpeg_destroy(canvas)" },
  { slug: "vhs-halation", inputs: 3, name: "VHS Halation", description: "Three images CRT/VHS filtered — scan lines, color bleed, tracking errors — then composited throug...", effects: "crt_vhs(a) → crt_vhs(b) → ancient_halationedge(c,a,b→canvas) → channel_shift(canvas)" },
  { slug: "voronoi-chimera", inputs: 3, name: "Voronoi Chimera", description: "Faces and forms fused at Voronoi cell boundaries.", effects: "deepdream(a) → pixel_sort(b) → dither(c) → fragment(a,b,c→canvas) → blend(canvas,a→canvas) → channel_shift(canvas) → jpeg_destroy(canvas)" },
  { slug: "wax-museum", inputs: 3, name: "Wax Museum", description: "Three images each passed through style transfer — their surfaces repainted in the manner of some...", effects: "style_transfer(a) → style_transfer(b) → style_transfer(c) → blend(a,b→canvas) → blend(canvas,c→canvas) → seam_carve(canvas) → pixel_sort(canvas) → jpeg_destroy(canvas)" },
  { slug: "wobble-curvyscreen-dither", inputs: 4, name: "Wobble Curvy Screen Dither", description: "Four images: two wobble-eyed into drifting off-axis rings, then curvy-line-screened against a third.", effects: "ancient_wobbleeye(a) → ancient_wobbleeye(b) → ancient_curvylinescreen(c,a,b→canvas) → mask_composite(canvas,d→canvas) → dither(canvas)" },
  { slug: "wobble-halation", inputs: 3, name: "Wobble Halation", description: "Two images wobble-eyed — off-axis concentric rings drifting from center — then composited through...", effects: "ancient_wobbleeye(a) → ancient_wobbleeye(b) → ancient_halationedge(c,a,b→canvas)" },
  { slug: "x-ray-composite", inputs: 2, name: "X-Ray Composite", description: "One image is decomposed to its principal components — stripped to structural essentials, the eige...", effects: "pca_decompose(a) → mask_composite(b,a→canvas) → blend(canvas,a→canvas) → spectral(canvas) → dither(canvas) → jpeg_destroy(canvas)" },
] as const;

const slugSet = new Set(RECIPES.map((r) => r.slug));

/** Check whether a slug corresponds to a valid recipe. */
export function isValidRecipe(slug: string): boolean {
  return slugSet.has(slug);
}

/** Look up a recipe by slug, or undefined if not found. */
export function getRecipe(slug: string): Recipe | undefined {
  return RECIPES.find((r) => r.slug === slug);
}

/**
 * Format the full recipe list for Slack display, grouped by input count.
 * Uses Slack mrkdwn formatting.
 */
export function formatRecipeList(): string {
  const grouped = new Map<number, Recipe[]>();

  for (const recipe of RECIPES) {
    const list = grouped.get(recipe.inputs);
    if (list) {
      list.push(recipe);
    } else {
      grouped.set(recipe.inputs, [recipe]);
    }
  }

  const sortedKeys = [...grouped.keys()].sort((a, b) => a - b);

  const sections = sortedKeys.map((count) => {
    const recipes = grouped.get(count)!;
    const recipeLines = recipes.map((r) => `\`${r.slug}\` \u2014 ${r.name}`).join("\n");
    return `*${count} inputs:*\n${recipeLines}`;
  });

  return `*Available Recipes (${RECIPES.length} total):*\n\n${sections.join("\n\n")}`;
}

/**
 * Find recipes whose slugs contain the given substring.
 * Returns up to 5 matches for use in "did you mean?" suggestions.
 */
export function suggestRecipes(input: string): Recipe[] {
  const lower = input.toLowerCase();
  return RECIPES.filter((r) => r.slug.includes(lower)).slice(0, 5);
}
