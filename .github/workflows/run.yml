name: Run Collage Bot

on:
  schedule:
    - cron: '0 9 * * *'  # 9am UTC daily
  workflow_dispatch:
    inputs:
      source_channel:
        description: 'Channel to fetch images from'
        default: 'image-gen'
      post_channel:
        description: 'Channel to post results to'
        default: 'img-junkyard'
      num_images:
        description: 'Number of source images'
        default: '4'
      split:
        description: 'Transform split ratio (0.0–0.5)'
        default: '0.25'
      blend_width:
        description: 'Seam blend width (px)'
        default: '70'

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run collage bot
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
        run: |
          python bot.py \
            --source-channel "${{ inputs.source_channel || 'image-gen' }}" \
            --post-channel "${{ inputs.post_channel || 'img-junkyard' }}" \
            --num-images "${{ inputs.num_images || '4' }}" \
            --split "${{ inputs.split || '0.25' }}" \
            --blend-width "${{ inputs.blend_width || '70' }}"
